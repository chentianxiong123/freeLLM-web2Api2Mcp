[project]
name = "deepseek-web-agent"
version = "0.3.0"
description = "DeepSeek Web Agent Proxy"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.138",
    "uvicorn>=0.49",
    "curl-cffi>=0.15",
]
